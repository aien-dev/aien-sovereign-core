[package]
name = "e2e_tests"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
aien-inference-abi = { path = "../crates/aien-inference-abi" }
tokio = { version = "1.37", features = ["sync", "rt", "macros"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
