[package]
name = "aien-security"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Normative security abstractions, SecretProvider traits, and network purpose governance for AIEN"

[dependencies]
zeroize = { version = "1.9", features = ["zeroize_derive"] }
tracing = "0.1"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
