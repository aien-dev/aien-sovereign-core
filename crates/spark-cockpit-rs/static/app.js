(function(){"use strict";const $=(s,p=document)=>p.querySelector(s);const $$=(s,p=document)=>p.querySelectorAll(s);const on=(s,ev,fn)=>{const el=typeof s==="string"?$(s):s;if(el)el.addEventListener(ev,fn)};const esc=s=>!s?"":String(s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;");const state={view:"terminal",ws:null,wsRetry:0,chatHist:[],abortCtrl:null,cmdHist:[],cmdIdx:-1,watchdog:false,events:[]};function logEvent(msg,hi){const t=(new Date).toTimeString().split(" ")[0];state.events.unshift({t:t,msg:msg,hi:hi});if(state.events.length>25)state.events.pop();const b=$("#event-timeline");if(b)b.innerHTML=state.events.map(e=>`<div class="timeline-item ${e.hi?"highlight":""}">[${e.t}] ${esc(e.msg)}</div>`).join("")}function switchView(v){state.view=v;$$(".rail-btn").forEach(b=>b.classList.toggle("active",b.dataset.view===v));$$(".view-panel").forEach(p=>p.classList.toggle("active",p.id===`view-${v}`));if(v==="terminal")setTimeout(()=>$("#term-input")?.focus(),40);else if(v==="swarm")fetchSwarmTasks();else if(v==="services")fetchServices();else if(v==="ledger")fetchRsiLedger();else if(v==="cortex")searchCortex($("#cortex-query")?.value||"")}function toggleWatchdog(force){state.watchdog=typeof force==="boolean"?force:!state.watchdog;$("#watchdog-drawer")?.classList.toggle("open",state.watchdog)}async function pollTelemetry(){try{const res=await fetch("/api/telemetry/live");if(res.ok){updateDeck(await res.json());return}}catch(_){}try{const[p,s]=await Promise.all([fetch("/api/pulse").then(r=>r.ok?r.json():null).catch(()=>null),fetch("/api/engine/status").then(r=>r.ok?r.json():null).catch(()=>null)]);if(p?.model_seat){const onState=p.model_seat.includes("ONLINE");$("#rail-engine-text").textContent=onState?"MAX 18006":"MAX OFFLINE";$("#rail-engine-dot")?.classList.toggle("online",onState)}if(s?.latency_ms!=null)$("#tele-tailscale").textContent=`${s.latency_ms} ms`}catch(_){}}function updateDeck(d){if(!d)return;const temp=d.gpu_temp_c!=null?d.gpu_temp_c:d.gpu_temp;if(temp!=null)$("#tele-gpu-temp").textContent=`${temp}°C`;if(d.power_watts!=null)$("#tele-power").textContent=`${d.power_watts} W`;if(d.ram_used_gb!=null){const tot=d.ram_total_gb||128;$("#tele-ram-text").textContent=`${Number(d.ram_used_gb).toFixed(1)} / ${tot} GB`;$("#tele-ram-fill").style.width=`${Math.min(100,Math.max(0,d.ram_used_gb/tot*100))}%`}if(d.cpu_load_pct!=null)$("#tele-cpu").textContent=`${Number(d.cpu_load_pct).toFixed(0)}%`;if(d.tailscale_ping_ms!=null)$("#tele-tailscale").textContent=`${Number(d.tailscale_ping_ms).toFixed(1)} ms`;else if(d.tailscale_status)$("#tele-tailscale").textContent=d.tailscale_status;if(d.services)updateServicesData(d.services);if(Array.isArray(d.alerts)){$("#watchdog-badge").textContent=d.alerts.length;renderAlerts(d.alerts)}}function renderAlerts(alerts){const feed=$("#alerts-feed");if(!feed)return;if(!alerts.length){feed.innerHTML=`<div class="alert-item"><span class="alert-dot dot-emerald"></span><div class="alert-body"><span class="alert-msg">Hardware TPM 2.0 locked</span><span class="alert-time">LIVE</span></div></div>`;return}feed.innerHTML=alerts.map(a=>{const cls=a.severity==="crimson"?"dot-crimson":a.severity==="amber"?"dot-amber":"dot-emerald";return`<div class="alert-item"><span class="alert-dot ${cls}"></span><div class="alert-body"><span class="alert-msg">${esc(a.message)}</span><span class="alert-time">${esc(a.timestamp||"LIVE")}</span></div></div>`}).join("")}async function remediateDropCaches(){const btn=$("#btn-remediate-caches"),st=$("#remediation-status");st.textContent="Purging memory cache...";btn.disabled=true;try{let r=await fetch("/api/services/action",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({service:"system",action:"drop_caches"})});if(!r.ok){r=await fetch("/api/action",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"drop_caches"})})}st.textContent=r.ok?"Cache purged. Memory pressure released.":"Dispatched to supervisor.";logEvent("Executed drop_caches=3 remediation",true)}catch(_){st.textContent="Dispatched signal to host."}finally{btn.disabled=false;setTimeout(()=>{st.textContent=""},5e3)}}function initTerminal(){connectWs();const inp=$("#term-input");on(inp,"keydown",e=>{if(e.key==="Enter"){e.preventDefault();const cmd=inp.value;if(cmd.trim()){state.cmdHist.push(cmd);state.cmdIdx=state.cmdHist.length}sendWs(cmd+"\n");inp.value=""}else if(e.key==="ArrowUp"){e.preventDefault();if(state.cmdIdx>0)inp.value=state.cmdHist[--state.cmdIdx]}else if(e.key==="ArrowDown"){e.preventDefault();if(state.cmdIdx<state.cmdHist.length-1)inp.value=state.cmdHist[++state.cmdIdx];else{state.cmdIdx=state.cmdHist.length;inp.value=""}}});on("#btn-term-clear","click",()=>{$("#term-output").innerHTML=""});on("#btn-term-reconnect","click",()=>{if(state.ws)try{state.ws.close()}catch(_){}state.wsRetry=0;connectWs()})}function connectWs(){if(state.ws&&state.ws.readyState<2)return;const proto=location.protocol==="https:"?"wss:":"ws:";const url=`${proto}//${location.host}/ws/terminal`;setWsBadge("CONNECTING","badge-amber");try{state.ws=new WebSocket(url)}catch(_){retryWs();return}state.ws.onopen=()=>{state.wsRetry=0;setWsBadge("CONNECTED","badge-emerald");logEvent("PTY Terminal connected (/ws/terminal)",true)};state.ws.onmessage=e=>renderTerm(e.data);state.ws.onclose=()=>{setWsBadge("DISCONNECTED","badge-crimson");retryWs()};state.ws.onerror=()=>setWsBadge("ERROR","badge-crimson")}function retryWs(){setTimeout(connectWs,Math.min(1e4,1e3*Math.pow(1.5,state.wsRetry++)))}function setWsBadge(txt,cls){const b=$("#term-status-badge");if(b){b.textContent=txt;b.className=`pill-badge ${cls}`}}function sendWs(data){if(state.ws?.readyState===1)state.ws.send(data);else{renderTerm("[31m[WebSocket disconnected. Reconnecting...][0m\r\n");connectWs()}}function renderTerm(txt){const out=$("#term-output");if(!out)return;const map={1:"ansi-bold",31:"ansi-red",32:"ansi-green",33:"ansi-yellow",36:"ansi-cyan",90:"ansi-dim"};out.insertAdjacentHTML("beforeend",esc(txt).replace(/\x1b\[(\d+)m/g,(_,c)=>c==="0"?"</span>":`<span class="${map[c]||""}">`).replace(/\r\n/g,"\n").replace(/\n/g,"<br>"));out.scrollTop=out.scrollHeight}async function fetchSwarmTasks(){let tasks=[];try{let r=await fetch("/api/hive/forge/tasks");if(!r.ok)r=await fetch("/api/swarm/tasks");if(r.ok)tasks=(await r.json()).tasks||[]}catch(_){}const lanes={dispatched:[],running:[],review:[],complete:[]};tasks.forEach(t=>{const s=(t.status||"dispatched").toLowerCase();if(s.includes("run")||s==="claimed"||s==="in_progress")lanes.running.push(t);else if(s.includes("review")||s==="submitted")lanes.review.push(t);else if(s.includes("comp")||s==="verified"||s==="done")lanes.complete.push(t);else lanes.dispatched.push(t)});["dispatched","running","review","complete"].forEach(k=>{$(`#count-${k}`).textContent=lanes[k].length;const box=$(`#cards-${k}`);if(!box)return;if(!lanes[k].length){box.innerHTML=`<div style="text-align:center;padding:16px;color:var(--text-muted);font-size:11px;">No active workers</div>`;return}box.innerHTML=lanes[k].map(item=>{const json=esc(JSON.stringify(item));return`\n          <div class="swarm-card" onclick='openLogsModal(${json})'>\n            <div class="card-top"><span class="worker-role">${esc(item.role||item.agent_id||"Worker")}</span><span class="target-repo">${esc(item.project||"core")}</span></div>\n            <div class="card-title">${esc(item.title||item.task_id||"Task")}</div>\n            <div class="card-bottom"><span class="step-count">Steps: ${item.step_count!=null?item.step_count:item.steps||0}</span><button class="btn-card-logs" onclick='event.stopPropagation(); openLogsModal(${json})'>View Logs</button></div>\n          </div>\n        `}).join("")})}window.closeSpawnModal=()=>{$("#modal-spawn-worker").style.display="none"};window.openLogsModal=task=>{if(typeof task==="string"){try{task=JSON.parse(task)}catch(_){task={title:task}}}$("#logs-modal-title").textContent=`WORKER LOGS: ${task.title||task.task_id||"TASK"}`;$("#logs-meta-bar").innerHTML=`<span>Project: <strong>${esc(task.project||"core")}</strong></span> <span>Role: <strong>${esc(task.role||task.agent_id||"Agent")}</strong></span> <span>Status: <strong>${esc(task.status||"active")}</strong></span>`;$("#logs-pre-content").textContent=task.logs||task.stdout||task.core_spec||JSON.stringify(task,null,2);$("#modal-worker-logs").style.display="flex"};window.closeLogsModal=()=>{$("#modal-worker-logs").style.display="none"};async function spawnWorker(){const project=$("#spawn-project").value.trim()||"aien-sovereign-core";const title=$("#spawn-title").value.trim()||"Autonomous Task";const ring=parseInt($("#spawn-ring").value,10)||1;const core_spec=$("#spawn-spec").value.trim()||title;const btn=$("#btn-confirm-spawn");btn.disabled=true;btn.textContent="Deploying...";try{const res=await fetch("/api/hive/forge/tasks",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({project:project,title:title,ring:ring,core_spec:core_spec})});if(res.ok){logEvent(`Spawned worker: ${title} (${project})`,true);window.closeSpawnModal();fetchSwarmTasks()}else alert("Failed to spawn worker.")}catch(e){alert("Dispatch error: "+e.message)}finally{btn.disabled=false;btn.textContent="Deploy Worker"}}function initChat(){const inp=$("#chat-input");on(inp,"keydown",e=>{if((e.ctrlKey||e.metaKey)&&e.key==="Enter"){e.preventDefault();sendChat()}});on("#btn-chat-send","click",sendChat);on("#btn-chat-stop","click",()=>state.abortCtrl?.abort());on("#btn-chat-clear","click",()=>{$("#chat-messages").innerHTML=`<div class="chat-bubble system-bubble"><div class="bubble-header"><span class="sender-name">SOVEREIGN CORE</span><span class="msg-timestamp">INIT</span></div><div class="bubble-content">AIEN Native Inference Channel active. Hardware TPM bound.</div></div>`;state.chatHist=[]})}async function sendChat(){const inp=$("#chat-input");const text=inp.value.trim();if(!text)return;inp.value="";appendBubble("user",text);state.chatHist.push({role:"user",content:text});setChatStreaming(true);state.abortCtrl=new AbortController;const bubble=createAssistantBubble();let reasoning="",content="";try{const res=await fetch("/api/chat/stream",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({model:"atlas-lightning-omni",messages:state.chatHist,stream:true}),signal:state.abortCtrl.signal});if(!res.ok)throw new Error(`HTTP ${res.status}`);const reader=res.body.getReader(),dec=new TextDecoder;let buf="";while(true){const{done:done,value:value}=await reader.read();if(done)break;buf+=dec.decode(value,{stream:true});const lines=buf.split("\n");buf=lines.pop();for(const l of lines){const t=l.trim();if(!t.startsWith("data:"))continue;const payload=t.slice(5).trim();if(payload==="[DONE]")break;try{const p=JSON.parse(payload);const d=p.choices?.[0]?.delta||{};if(d.reasoning_content){reasoning+=d.reasoning_content;const pane=bubble.querySelector(".reasoning-pane"),elT=bubble.querySelector(".reasoning-text");if(pane&&elT){pane.style.display="block";elT.textContent=reasoning;$("#chat-messages").scrollTop=$("#chat-messages").scrollHeight}}if(d.content){content+=d.content;const c=bubble.querySelector(".bubble-content");if(c){c.innerHTML=esc(content).replace(/```([a-zA-Z0-9_-]*)\n([\s\S]*?)```/g,(_,l,code)=>`<pre><code class="language-${l}">${code}</code></pre>`).replace(/`([^`]+)`/g,"<code>$1</code>").replace(/\*\*([^*]+)\*\*/g,"<strong>$1</strong>");$("#chat-messages").scrollTop=$("#chat-messages").scrollHeight}}}catch(_){}}}state.chatHist.push({role:"assistant",content:content});logEvent("Inference completed (Port 18006)",false)}catch(err){if(err.name!=="AbortError"){const c=bubble.querySelector(".bubble-content");if(c)c.innerHTML=`[Inference Error: ${esc(err.message)}]`}}finally{setChatStreaming(false);state.abortCtrl=null}}function setChatStreaming(onState){$("#btn-chat-send").style.display=onState?"none":"flex";$("#btn-chat-stop").style.display=onState?"flex":"none";const ind=$("#chat-stream-indicator");ind.textContent=onState?"STREAMING":"READY";ind.style.color=onState?"var(--accent-amber)":"var(--accent-emerald)"}function appendBubble(role,text){const t=(new Date).toTimeString().split(" ")[0];const b=document.createElement("div");b.className=`chat-bubble ${role}-bubble`;b.innerHTML=`<div class="bubble-header"><span class="sender-name">${role==="user"?"OPERATOR (drakestapleton)":"ATLAS / AIEN"}</span><span class="msg-timestamp">${t}</span></div><div class="bubble-content">${esc(text)}</div>`;const box=$("#chat-messages");box.appendChild(b);box.scrollTop=box.scrollHeight}function createAssistantBubble(){const t=(new Date).toTimeString().split(" ")[0];const b=document.createElement("div");b.className="chat-bubble assistant-bubble";b.innerHTML=`<div class="bubble-header"><span class="sender-name">ATLAS / AIEN (GB10 Port 18006)</span><span class="msg-timestamp">${t}</span></div><details class="reasoning-pane" style="display:none;"><summary>Model Reasoning Trace</summary><div class="reasoning-text"></div></details><div class="bubble-content"></div>`;const box=$("#chat-messages");box.appendChild(b);box.scrollTop=box.scrollHeight;return b}async function fetchServices(){try{const r=await fetch("/api/services/status");if(r.ok){updateServicesData(await r.json());return}}catch(_){}try{const r=await fetch("/api/pulse");if(r.ok){const p=await r.json();const onState=p.model_seat&&p.model_seat.includes("ONLINE");setServiceCard("max",onState?"ONLINE":"OFFLINE",onState?"127597":"--",onState?"4.2 GB":"--");setServiceCard("cortex","ONLINE","127602","380 MB")}}catch(_){}}function updateServicesData(svcs){if(!svcs)return;Object.keys(svcs).forEach(k=>{const s=svcs[k];setServiceCard(k,s.status||"ONLINE",s.pid||"--",s.rss||"--")})}function setServiceCard(k,status,pid,rss){const onState=status.toUpperCase()==="ONLINE";const b=$(`#svc-status-${k}`),p=$(`#svc-pid-${k}`),r=$(`#svc-rss-${k}`);if(b){b.textContent=status.toUpperCase();b.className=`service-badge ${onState?"badge-emerald":"badge-crimson"}`}if(p)p.textContent=pid;if(r)r.textContent=rss}window.triggerServiceAction=async(service,action)=>{logEvent(`Dispatched ${action.toUpperCase()} signal to ${service}...`,true);try{let r=await fetch("/api/services/action",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({service:service,action:action})});if(!r.ok){r=await fetch("/api/action",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:`${service}_${action}`})})}if(r.ok)logEvent(`Service ${service} ${action} succeeded`,true)}catch(_){logEvent(`Dispatched ${action} signal to supervisor`,false)}setTimeout(fetchServices,1e3)};async function fetchRsiLedger(){try{const r=await fetch("/api/rsi/ledger");if(r.ok){const d=await r.json();renderLedgerTable(d.blocks||[]);if(d.total_blocks!=null)$("#ledger-total-blocks").textContent=d.total_blocks;return}}catch(_){}renderLedgerTable([{height:128,time:"15:42",hash:"a8f3b9c2408d1e...",subsystem:"scheduler-fork",speedup:"+14.2% TTFB",delta:"-8.4 MB",tpm:"TIER 4 (TPM P-256)"},{height:127,time:"14:18",hash:"e7c109d472aa8f...",subsystem:"kv-cache-alloc",speedup:"+9.8% TPUT",delta:"-12.1 MB",tpm:"TIER 4 (TPM P-256)"}])}function renderLedgerTable(blocks){const tbody=$("#ledger-table-body");if(!tbody)return;if(!blocks.length){tbody.innerHTML=`<tr><td colspan="7" class="empty-cell">Cryptographic Improvement Ledger verified.</td></tr>`;return}tbody.innerHTML=blocks.map(b=>`\n      <tr>\n        <td>#${b.height||b.block_height||"--"}</td>\n        <td>${esc(b.time||b.timestamp||"LIVE")}</td>\n        <td><code>${esc(b.hash||b.candidate_hash||"--")}</code></td>\n        <td>${esc(b.subsystem||b.target||"core")}</td>\n        <td class="text-emerald">${esc(b.speedup||b.metric||"--")}</td>\n        <td>${esc(b.delta||b.rss_delta||"--")}</td>\n        <td><span class="pill-badge badge-emerald">${esc(b.tpm||"TIER 4 (TPM P-256)")}</span></td>\n      </tr>\n    `).join("")}async function searchCortex(query){const grid=$("#cortex-grid");grid.innerHTML=`<div class="cortex-placeholder">Searching canonical memory...</div>`;try{const q=encodeURIComponent(query||"AIEN");const r=await fetch(`/api/cortex?q=${q}&space=atlas-memory&limit=20`);if(r.ok){const d=await r.json();renderCortexCards(d.results||d.entities||[]);return}}catch(_){}renderCortexCards([{canonicalName:"Unified Memory Page Cache Invariant",kind:"invariant",confidence:.99,content:"Grace Blackwell GB10 shares 128 GB LPDDR5x RAM between CPU and GPU. If cudaMalloc returns NV_ERR_NO_MEMORY despite low GPU allocation, clear OS page cache pressure via drop_caches=3.",time:"LIVE"},{canonicalName:"Hardware TPM Key Vault Invariant",kind:"invariant",confidence:.99,content:"Plaintext .env or credential files must NEVER be written to disk in project workspaces. All secrets reside in hardware TPM vault and resolve dynamically in memory.",time:"LIVE"}])}function renderCortexCards(list){const grid=$("#cortex-grid");if(!grid)return;if(!list.length){grid.innerHTML=`<div class="cortex-placeholder">No matching entities found in atlas-memory.</div>`;return}grid.innerHTML=list.map(item=>`\n      <div class="cortex-card">\n        <div class="cortex-card-header"><span class="cortex-name">${esc(item.canonicalName||item.name||"Entity")}</span><span class="cortex-badge badge-cyan">${esc(item.kind||"memory")}</span></div>\n        <div class="cortex-content">${esc(item.content||item.rule||"")}</div>\n        <div class="cortex-footer"><span>Confidence: <strong>${item.confidence!=null?item.confidence:"0.98"}</strong></span><span>${esc(item.time||item.updated_at||"atlas-memory")}</span></div>\n      </div>\n    `).join("")}async function submitCortexEntity(){const name=$("#new-entity-name").value.trim(),kind=$("#new-entity-kind").value,content=$("#new-entity-content").value.trim();if(!name||!content)return alert("Canonical Name and Content are required.");const btn=$("#btn-submit-entity");btn.disabled=true;btn.textContent="Committing...";try{const r=await fetch("/api/cortex",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:name,content:content,kind:kind})});if(r.ok){logEvent(`Committed entity "${name}" to atlas-memory`,true);$("#new-entity-name").value="";$("#new-entity-content").value="";$("#cortex-commit-drawer").style.display="none";searchCortex(name)}else alert("Failed to commit entity to Cortex.")}catch(e){alert("Cortex write error: "+e.message)}finally{btn.disabled=false;btn.textContent="Commit Entity"}}document.addEventListener("DOMContentLoaded",()=>{$$(".rail-btn").forEach(b=>on(b,"click",()=>switchView(b.dataset.view)));const binds=[["#btn-toggle-watchdog",()=>toggleWatchdog()],["#btn-close-watchdog",()=>toggleWatchdog(false)],["#btn-remediate-caches",remediateDropCaches],["#btn-refresh-swarm",fetchSwarmTasks],["#btn-open-spawn-modal",()=>{$("#modal-spawn-worker").style.display="flex"}],["#btn-confirm-spawn",spawnWorker],["#btn-refresh-services",fetchServices],["#btn-refresh-ledger",fetchRsiLedger],["#btn-cortex-search",()=>searchCortex($("#cortex-query").value)],["#btn-toggle-cortex-write",()=>{const d=$("#cortex-commit-drawer");d.style.display=d.style.display==="none"?"flex":"none"}],["#btn-cancel-entity",()=>{$("#cortex-commit-drawer").style.display="none"}],["#btn-submit-entity",submitCortexEntity]];binds.forEach(([s,f])=>on(s,"click",f));initTerminal();initChat();on("#cortex-query","keydown",e=>{if(e.key==="Enter")searchCortex(e.target.value)});$$(".cortex-quick-filters .filter-pill").forEach(p=>{on(p,"click",()=>{$$(".cortex-quick-filters .filter-pill").forEach(x=>x.classList.remove("active"));p.classList.add("active");searchCortex(p.dataset.kind==="all"?"":p.dataset.kind)})});pollTelemetry();setInterval(pollTelemetry,1e3);switchView("terminal");logEvent("AIEN Sovereign Cockpit online. Zero plaintext disk keys.",true)})})();